# GitHub Personal Access Token (fine-grained, repo scope).
# Used for the GitHub MCP server and the gh CLI.
GITHUB_PAT=

# Optional. Set only if not using the Claude subscription for MCP auth.
# ANTHROPIC_API_KEY=
