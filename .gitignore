# Environment
.venv/
.venv-wsl/
venv/
env/
.env
.env.local
.env.*.local
!.env.example

# MCP config
.mcp.json

# Agent instruction files (local only, never pushed to remote)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
.cursorrules
.windsurfrules

# Session review artifacts (local only, findings never pushed to remote)
notes/session_04_7_*.md
notes/coderabbit_findings_*.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
*.code-workspace
.DS_Store
Thumbs.db

# Data and outputs — large, regenerable
data/*
outputs/*
!data/.gitkeep
!outputs/.gitkeep
# Dataset descriptors are small and belong in the repo; samples never do.
!data/dataset_spec.yaml
!data/dataset_summary.md

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.synctex.gz
paper/_minted-*

# Benchmark scratch (keep the results file)
benchmarks/scratch/

# Model weights
*.pt
*.ckpt
*.safetensors

# Local micromamba binary
bin/

# FFCx/dolfinx JIT cache (kept in-workspace via XDG_CACHE_HOME)
.jit-cache/
